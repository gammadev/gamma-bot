apply plugin: "org.sonarqube"

sonarqube {
    properties {
        property "sonar.projectName", "gamma-bot"
        property "sonar.projectKey", "gamma-bot"

        property "sonar.host.url", "http://localhost"
        property "sonar.login", System.getenv("SONAR_TOKEN")
    }
}
